{
  "case_id": "rc-003-missing-context",
  "name": "A required context variable is not supplied; runtime fails before the model call",
  "scenario": "missing-context-variable",
  "stage": "runtime",
  "expected": { "status": "failed", "reason": "missing_context_variable" },
  "payload": {
    "userVariables": {},
    "promptPackage": {
      "manifest": { "promptCompilerVersion": "1.0.0", "timestamp": "2026-07-15T00:00:00.000Z" },
      "promptSpecification": {
        "systemPrompt": "You are the IRR stage engine (regression).",
        "userPromptTemplate": "Investigation: {{investigation}}. Produce the brief.",
        "contextRequirements": ["investigation"],
        "targetRuntime": "smoke"
      },
      "outputSchema": {
        "required": ["executiveBrief"],
        "properties": { "executiveBrief": { "type": "string" } }
      }
    }
  }
}
