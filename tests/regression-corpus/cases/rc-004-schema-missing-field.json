{
  "case_id": "rc-004-schema-missing-field",
  "case_name": "Valid JSON that omits a required contract field fails schema validation",
  "scenario": "schema-missing-required-field",
  "stage": "runtime",
  "expected": { "status": "failed", "reason": "invalid_response_schema" },
  "payload": {
    "userVariables": { "investigation": "RC-004" },
    "injectResponse": "{}",
    "promptPackage": {
      "manifest": { "promptCompilerVersion": "1.0.0", "timestamp": "2026-07-15T00:00:00.000Z" },
      "promptSpecification": {
        "systemPrompt": "You are the IRR stage engine (regression).",
        "userPromptTemplate": "Investigation: {{investigation}}. Produce the brief.",
        "contextRequirements": ["investigation"],
        "targetRuntime": "smoke"
      },
      "outputSchema": {
        "required": ["executiveBrief"],
        "properties": { "executiveBrief": { "type": "string" } }
      }
    }
  }
}
